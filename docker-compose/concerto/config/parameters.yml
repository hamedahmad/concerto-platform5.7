parameters:
    database_driver: pdo_mysql
    database_host: database
    database_port: null
    database_name: concerto
    database_user: root
    database_password: root
    database_path: null
    database_unix_socket: null
    database_version: mysql:8.0
    router.request_context.base_url: ""
    router.request_context.http_port: '%env(int:OUTPUT_HTTP_PORT)%'
    router.request_context.https_port: '%env(int:OUTPUT_HTTPS_PORT)%'
    router.request_context.host: localhost
    router.request_context.scheme: http
    kernel.secret: '%env(DATABASE_SECRET)%'

    locale: de_DE
    # ✅ fix for php8.2
    database_options:
        1006: ''
